---
// Cal.com embed component using Astro Islands Architecture
// This component will only hydrate on the client side
---

<div
  style="width:100%;height:100%;overflow:scroll"
  id="my-cal-inline-30min"
></div>

<script>
  // This script runs only on the client side
  (function (C, A, L) {
    let p = function (a, ar) {
      a.q.push(ar);
    };
    let d = C.document;
    C.Cal =
      C.Cal ||
      function () {
        let cal = C.Cal;
        let ar = arguments;
        if (!cal.loaded) {
          cal.ns = {};
          cal.q = cal.q || [];
          d.head.appendChild(d.createElement("script")).src = A;
          cal.loaded = true;
        }
        if (ar[0] === L) {
          const api = function () {
            p(api, arguments);
          };
          const namespace = ar[1];
          api.q = api.q || [];
          if (typeof namespace === "string") {
            cal.ns[namespace] = cal.ns[namespace] || api;
            p(cal.ns[namespace], ar);
            p(cal, ["initNamespace", namespace]);
          } else p(cal, ar);
          return;
        }
        p(cal, ar);
      };
  })(
    window,
    "http://calcom-vo8kgwo88g448wkssgsoow8g.3.137.161.1.sslip.io/embed/embed.js",
    "init"
  );
  Cal("init", "30min", {
    origin: "http://calcom-vo8kgwo88g448wkssgsoow8g.3.137.161.1.sslip.io",
  });

  Cal.ns["30min"]("inline", {
    elementOrSelector: "#my-cal-inline-30min",
    config: { layout: "month_view", theme: "dark" },
    calLink: "wflows/30min",
  });

  Cal.ns["30min"]("ui", {
    theme: "dark",
    hideEventTypeDetails: false,
    layout: "month_view",
  });
</script>

